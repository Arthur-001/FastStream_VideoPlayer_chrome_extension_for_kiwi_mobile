# Custom Rules for FastStream Kiwi Mobile

## Project-Scoped Rules
- **Do not run build or install commands:** Never run `npm install`, `npm run build`, or any other packaging/build commands for this extension. The extension is loaded as an unpacked directory directly from the `chrome/` folder.
